# Core orchestration
langgraph>=0.2.40
langchain>=0.3.0
langchain-core>=0.3.0

# LLM providers (configurable — see src/burry/models.py)
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0  # default provider (LLM_PROVIDER=gemini)

# Market data + execution
alpaca-py>=0.33.0
finnhub-python>=2.4.20
massive>=1.0  # optional extra research source (see ENABLE_MASSIVE)

# Config / utils
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
